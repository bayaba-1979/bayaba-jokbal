name: 📰 Tistory RSS → 기자/ (bayaba-jokbal)

# 중앙 재사용 워크플로 호출 — 로직은 bayaba_phone/.github/workflows/tistory-sync.yml
on:
  schedule:
    - cron: '7 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    uses: bayaba-1979/bayaba_phone/.github/workflows/tistory-sync.yml@main
    with:
      blog: bayaba-jokbal
